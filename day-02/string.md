In Python, a string is a sequence of characters, enclosed within single (' '), double (" "), or triple (''' ''' or """ """) quotes.

eg :  Hostname = "router_lab1"

Strings are immutable, meaning you cannot change the characters within a string directly. Instead, you create new strings.

eg : Hostname = "router_lab1" this will create new varible in memory exsisting will cleanup concept garbage collection 

You can access individual characters in a string using indexing, e.g., string[0] will give you the first character.

eg : hostane = "router_lab1" we can hostname[0] give 'r'

